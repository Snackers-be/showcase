{#<div class="my-6">#}
    <a {{ attributes.defaults({
        class: "bg-secondary text-white my-6 px-6 py-2 rounded-sm"
    }) }}
            href="{% if route is not empty %} {{ path(route) }} {% else %} # {% endif %}"
    >
        {{ label }}
    </a>
{#</div>#}