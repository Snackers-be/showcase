<h4 class="text-lg font-bold mb-5">Services</h4>
<ul class="">
    {% for category in categories %}
        <li class="">
            {{ component('Link', {
                label: category.name,
                route: 'app_services',
                fragment: category.name,
                class: '!text-black/50 dark:!text-darkBlack/50 hover:!text-[#000000] dark:hover:!text-[#ffffff]'
            }) }}
        </li>
{#        <ul>#}
{#            {% for service in category.services %}#}
{#                <li class="relative pl-12 before:bg-icon-check before:bg-no-repeat before:content-[''] before:inline-block before:top-1/2 before:absolute before:left-0 before:-translate-y-1/4 before:w-4 before:h-4">#}
{#                    <a href="#">{{ service.name }}</a>#}
{#                </li>#}
{#            {% endfor %}#}
{#        </ul>#}
    {% endfor %}
</ul>

{#TO DO#}
{#ajouter une image au modele / admin crud#}
{#diviser la page en deux et mettre l'image a droite (pas sur mobile)#}